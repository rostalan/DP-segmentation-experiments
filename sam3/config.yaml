# SAM3 Interactive Video Segmentation Configuration

paths:
  input_video: "../res/videos/VID5.mkv"
  output_dir: "output/frames"

sampling:
  frame_interval: 10               # Process every frame for best tracking (was 15)
  start_frame: 10                  # First frame to process

inference:
  device: "cpu"                    # "cpu" or "cuda"
